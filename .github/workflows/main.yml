name: Nightly Backup

on:
  workflow_dispatch:

  schedule:
    - cron: '0 7 * * 6'

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Workflow running every Saturday at 7 AM"
