name: Nightly Backup

on:
  workflow_dispatch:

  schedule:
    - cron: '55 18 * * *'

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Workflow running every Saturday at 12:25 AM"
